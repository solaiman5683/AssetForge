# NPM configuration for publishing

# Ensure that the package-lock.json is included
package-lock=true

# Don't run npm scripts automatically during install
ignore-scripts=false

# Ensure strict SSL
strict-ssl=true

# Default access for the package (public or restricted)
access=public
